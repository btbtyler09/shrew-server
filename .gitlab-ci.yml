stages:
  - test

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  PIP_DISABLE_PIP_VERSION_CHECK: "1"

pytest:
  stage: test
  image: python:3.12-slim
  cache:
    key: pip-py312
    paths:
      - .cache/pip
  script:
    - pip install -e ".[dev]"
    - pytest tests/ -v
